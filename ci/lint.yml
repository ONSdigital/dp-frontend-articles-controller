---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-frontend-articles-controller

run:
  path: dp-frontend-articles-controller/ci/scripts/lint.sh